{
  "name": "@openmind/litelog",
  "version": "3.0.0",
  "description": "",
  "main": "dist/logger.umd.js",
  "repository": "https://github.com/openmindlab/lite-log.js",
  "scripts": {
    "build": "microbundle -f umd -o dist --name Logger --file logger --sourcemap n --compress false",
    "dev": "microbundle watch -f umd -o dist --name Logger --file logger --sourcemap n --compress false",
    "test": "jest --coverage"
  },
  "author": "openmind",
  "license": "ISC",
  "devDependencies": {
    "jest": "22.1.4",
    "microbundle": "https://github.com/fatshotty/microbundle"
  }
}
