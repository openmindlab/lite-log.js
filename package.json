{
  "name": "lite-log",
  "version": "2.0.1",
  "description": "",
  "main": "dist/logger.umd.js",
  "scripts": {
    "build": "microbundle -f umd -o dist --file Logger --sourcemap n --compress false",
    "dev": "microbundle watch -f umd -o dist --file Logger --sourcemap n --compress false",
    "test": "jest --coverage"
  },
  "author": "openmind",
  "license": "ISC",
  "devDependencies": {
    "jest": "^22.1.4",
    "microbundle": "https://github.com/fatshotty/microbundle"
  }
}
